package com.example.mody.domain.recommendation.repository.category;

import com.example.mody.domain.recommendation.entity.category.StyleCategory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StyleCategoryRepository extends JpaRepository<StyleCategory, Long> {
}
