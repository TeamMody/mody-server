package com.example.mody.domain.recommendation.repository.category;

import com.example.mody.domain.recommendation.entity.category.AppealCategory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AppealCategoryRepository extends JpaRepository<AppealCategory, Long> {
}
